[package]
name = "example-driver"
version = "0.1.0"
edition = "2021"
license = "BSD-3-Clause"

[lib]
crate-type = ["cdylib"]
name = "example_driver"

[profile.release]
opt-level = "s"
lto = true
panic = "abort"
